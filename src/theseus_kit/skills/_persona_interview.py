"""Skill: persona-interview — 采访需求方获取机器人用户画像。

Entry-point skill for building a new robot's user persona through a
one-on-one interview: professional domain, skills, daily work, and a
knowledge structure captured as three structured natural-language lists
(concepts / data fields / relations).  The .tfo knowledge-graph conversion
is deliberately outsourced to the ``write-tfonto`` skill.
"""

from __future__ import annotations

from ._registry import SkillDef

SKILL = SkillDef(
    name="persona-interview",
    description="通过采访需求方获取机器人用户画像：职业领域、专业技能、日常工作、知识结构（三张结构化清单）与能力草图候选。",
    content={
        "SKILL.md": """---
name: persona-interview
description: 通过采访需求方获取机器人用户画像——职业领域、专业技能、日常工作、知识结构（结构化自然语言清单）与能力草图（Function/Action 候选）。当用户提出搭建新机器人、需要明确机器人角色设定、要求收集用户画像或采访需求方时使用。产出画像文档，知识结构与能力草图交由 write-tfonto 转换为 .tfo 知识图谱。
---

# Persona Interview —— 机器人用户画像采访

## 为什么是「采访」而不是「填表」

一次抛出四个板块让需求方自己填，得到的一定是残缺或敷衍的答案——画像质量的决定性环节是**追问**：需求方第一次回答总是「大概其」，跟进问题（「这个领域里什么活是机器人不该碰的？」「这个任务多久发生一次？」）才能逼出边界与频次。因此本技能的主体是一问一答的采访流程，不是问卷模板。

知识结构不走自由散文，也不在采访现场写格式文件：**需求方用自然语言确认领域事实（三张清单），格式转换交由 write-tfonto 技能机械完成**——事实由人把关、格式由技能把关。理想产出形态见子资源 `references/persona-example.md`。

## 采访流程

### Step 0 开场：对齐产出物

- 说明最终交付：① 画像文档（职业领域 / 专业技能 / 日常工作 / 知识结构三张清单 / 能力草图候选）② 知识图谱 `.tfo` 文件（由 write-tfonto 从清单与草图转换）。
- 用一句话确认：机器人代号、面向谁、解决什么场景（细节留到各板块展开）。
- 给需求方看画像示例（`references/persona-example.md` 资源），对齐「填到什么粒度算合格」。

### Step 1 职业领域

**开放问题**：「这个机器人处在什么行业/领域？服务什么人群？」

**追问边界**（至少要问出）：

- 领域的上下位归属（如「基础教育 → 初中数学」而非笼统的「教育」）
- 明确**不做什么**（领域边界比领域本身更值钱）
- 领域内的行话/黑话（影响后续 trigger_words 与召回词的选择）

**闭环**：复述领域定义让需求方确认，再进入下一板块。

### Step 2 专业技能

**开放问题**：「这个领域的合格从业者，需要哪些专业技能？」

**追问**：

- 区分「机器人必须具备」与「nice-to-have」——必须项进画像正文，其余进备注
- 每个技能落到**可观察行为**：「会分层讲解」优于「教学能力强」（后者无法验证）
- 有没有必须掌握的工具/规范/标准
- 顺手标注每个技能的能力形态（**只读计算**还是**写入操作**）——它们是日后 Function/Action 的候选（见 Step 5）

### Step 3 日常工作

**开放问题**：「从业者的一天/一周在做什么？」

**追问出频次与输入输出**——这是日后机器人工单与验收的来源：

| 任务 | 频次 | 输入 | 输出 |
|---|---|---|---|
| … | … | … | … |

每个任务问：多久一次？拿到什么？交付什么？做错的代价是什么？

**顺手标注写操作**：任务里「记一笔、改状态、更新记录」的部分单独标出——它们是日后 ActionDef 的候选。每个写操作追问两点：

- **连带变更**：「这个动作一次会同时改哪几处属性/关系？」——改多处的操作（如「记一次故障」= 改标题/状态/时间 + 关联实现 + 关联来源）正是 Action 相对逐个改属性的价值所在，记进候选备注
- **故障经验**：「过去真出过错吗？怎么补救的？怎么防复发？」——补救与防复发措施是日后召回与回归验收的依据

### Step 4 知识结构（三张清单）

这是唯一不产出自由文本的板块，但**也不直接写 .tfo**——需求方确认事实、write-tfonto 负责格式。采集三张清单：

1. **概念清单（名词）**：领域里有哪些实体？每个给一句话定义。例：`学生——接受教学服务的个体`。
2. **数据字段清单**：系统要「记住」每个概念的什么？带类型。例：`学生：姓名（文本）、学号（文本）、年级（整数）`。
3. **关系清单（动词）**：概念之间怎么连？方向 + 触发词（用户日常怎么描述这个关系）。例：`学生 → 知识点：掌握/擅长/熟练（正向）、薄弱/总错（负向）`。

**采集要领**：

- 关系必须是**动词**：需求方说「学生和错题有关」要追成「学生做错题目」还是「题目考察知识点」
- 触发词问「用户会怎么说」：它是日后召回下钻的判定键，每条关系收集 2-3 个口语化说法
- 数据字段问「日后系统要记住它什么」，不追求穷举——画像阶段粗粒度即可；有历史/有记录的概念补三轴：**时间**（何时发生）、**状态**（如何流转，状态值写进枚举）、**来源**（谁/哪条记录记载，可信度如何）
- **追问状态流转**：对有状态的概念（需求、决策、故障……）问「状态怎么变？什么动作触发？」——流转动作是 ActionDef 的天然候选，进 Step 5
- 明确**不纳入**的概念同样记下来，写进画像的边界

**闭环**：把三张清单复述给需求方确认——这是画像的知识质量门，宁可慢不可省。

### Step 5 能力草图（候选清单）

画像与机器人能力**天然同构**：专业技能里的可观察行为就是 query FunctionDef 的候选，日常工作里的写操作就是 ActionDef 的候选——采访顺手把它们捞出来，而不是留到功能开发时重新挖掘。按场景组合表（见 write-tfonto 技能的 `references/capability-layer.md`）给每个候选标注形态：

| 能力来源 | 候选形态 |
|---|---|
| 只读技能（诊断/汇总/推荐） | query FunctionDef 单用 |
| 简单写操作（记一笔事实） | ActionDef + edit_set 单用 |
| 复杂写操作（先查、再算、再改） | ActionDef(backing) + edit Function 组合 |
| 状态流转 / 连带多变更（一次改多处） | ActionDef + edit_set 多操作模板 |
| 改完要联动（通知/级联） | side_effects |

产出**第四张清单**（能力 → 形态 → 输入输出），复述给需求方确认。没有确认的能力不写进草图。

### Step 6 汇总落盘

- 输出画像文档到工作区 `~/.theseus/workspaces/<slug>/<slug>.md`（slug = 当前配置 `THESEUS_CREDENTIAL__ROBOT_PUBLIC_ID` 的 `:`→`-` 清洗），路径与需求方确认。
- 画像文档按 theseus 技能的 `references/persona-format.md` 规范格式组织（历史五部分结构见 `references/persona-example.md`）；知识图谱定义章节放三张清单全文与能力草图候选清单。
- 随后读取 write-tfonto 技能（`skill://com.a2c-smcp.theseus-kit/write-tfonto`），把三张清单与能力草图转换为 `.tfo`，放工作区 `configs/`，与画像文档一并交付。

## 采访纪律

1. **一次只问一个板块**，每板块以「复述确认」闭环后再进入下一板块。
2. **追问目的是边界与频次**（不做什么 / 多久一次 / 输入输出），不是让需求方自由发挥更多形容词。
3. **知识结构交付三张清单**（概念 / 数据字段 / 关系带触发词）、**能力交付候选清单**（形态由场景组合表决定）——不接受自由散文，也不在采访阶段直接写 .tfo。
4. **画像写「可验证的能力」，不写「口号」**：技能必须能落到具体任务或行为。
5. **状态流转与连带变更必问**：对「改状态、记一笔」类回答，必追问「一次改几处」——不问就丢了 Action 相对逐个改属性的最大价值。

## Resources

- `references/persona-example.md` — 用户画像示例（四部分理想产出形态，知识结构为三张清单）
""",
        "references/persona-example.md": """# 用户画像示例：初中数学教学机器人

> persona-interview 采访的理想产出形态。五部分结构与最终交付一一对应；知识结构部分为三张结构化清单
> （转换结果见 write-tfonto 技能的 `references/teacher-math-example.md`），能力草图见其
> `references/capability-layer.md` 范例。

## 机器人定位

面向初中数学教师的课堂外教学助手：批改分析、分层答疑、出题组卷、学情跟踪。服务对象为 7–9 年级数学教师与其学生，落地在课后辅导与作业场景。

## 1. 职业领域

- **领域**：基础教育 → 初中数学（7–9 年级）
- **边界（明确不做）**：不涉及高中内容（函数、导数等）；不承担课堂授课本身；不做心理辅导与家校沟通
- **行话示例**：错因、薄弱点、知识缺口、变式题、分层作业、单元目标

## 2. 专业技能（机器人必须具备）

| 技能 | 可观察行为 |
|---|---|
| 分层讲解 | 同一知识点按学生水平给出不同深度的讲法（基础版/进阶版） |
| 错因诊断 | 从错题反推具体知识缺口，输出「错在哪 → 缺什么 → 补什么」 |
| 出题组卷 | 按知识点/难度/题型组合生成练习与试卷 |
| 学情跟踪 | 汇总错题与成绩，输出学生/班级维度的学情报告 |
| 备课支持 | 按单元目标整理讲义、例题与作业素材 |

> nice-to-have（不入画像正文）：竞赛辅导、跨学科项目。

## 3. 日常工作

| 任务 | 频次 | 输入 | 输出 |
|---|---|---|---|
| 作业批改分析 | 每日 | 作业照片/电子作业 | 错题报告 + 薄弱点列表 |
| 课后答疑 | 高频 | 学生提问 | 分层解答 |
| 单元测试命题 | 每单元 | 教学进度 + 单元目标 | 试卷 + 答案 |
| 学情报告 | 每月 | 错题/成绩数据 | 班级学情报告 |
| 备课资料整理 | 每周 | 教材/教参 | 讲义与例题素材 |

## 4. 知识结构（三张清单）

**概念清单**：

- 学生——接受教学服务的个体
- 知识点——教学大纲中的最小教学单元
- 题目——练习或考试中的习题
- 单元——教材的章节组织单位

**数据字段清单**：

| 概念 | 字段（类型） |
|---|---|
| 学生 | 姓名（文本）、学号（文本）、年级（整数） |
| 知识点 | 名称（文本）、难度（文本：易/中/难） |
| 题目 | 标题（文本）、难度（文本：易/中/难） |
| 单元 | 名称（文本） |

**关系清单**：

| 关系 | 方向 | 触发词 |
|---|---|---|
| 掌握得好 | 学生 → 知识点 | 掌握、擅长、熟练 |
| 薄弱 | 学生 → 知识点 | 薄弱、不擅长、总错 |
| 考察 | 知识点 → 题目 | 覆盖、考察 |
| 隶属于 | 知识点 → 单元 | 属于、隶属 |
| 前置于 | 知识点 → 知识点 | 前置知识、先学 |

> 本清单转换为 `.tfo` 的结果见 write-tfonto 技能的 `references/teacher-math-example.md` 资源。
> 状态流转（画像新采集项）：学生-知识点掌握状态 无记录→薄弱→掌握，触发动作即下方「记一条薄弱点」「刷新掌握度」。

## 5. 能力草图（候选）

专业技能/日常工作映射出的 Function/Action 候选（完整范例见 write-tfonto 技能的 `references/capability-layer.md`）：

| 能力 | 来源 | 候选形态 |
|---|---|---|
| 错因诊断（错题 → 薄弱点列表） | 专业技能 | query FunctionDef 单用 |
| 记一条薄弱点（学生 + 知识点） | 课后答疑/批改 | ActionDef + edit_set 单用 |
| 刷新掌握度（先查错题、再算薄弱点、一次刷多条掌握边） | 学情跟踪 | ActionDef(backing) + edit Function 组合 |
""",
    },
)
